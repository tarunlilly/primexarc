# primedata DAG/runtime deps
loguru==0.7.3
minio==7.2.20
pydantic-settings>=2.0,<3

# Document parsing libraries
pypdf>=4.0,<6  # PDF files
python-pptx>=0.6.21,<1.0.0  # PowerPoint files
python-docx>=1.1.0,<2.0.0  # Word documents
reportlab>=4.0.0,<5.0.0  # PDF generation from DOCX content
lxml>=4.9.0,<6.0.0  # XML parsing
beautifulsoup4>=4.12.0,<5.0.0  # HTML and XML parsing
regex==2023.8.*  # Advanced regex support

# Data processing & visualization
pandas>=2.0.0,<3.0.0  # Data analysis and manipulation
numpy<2.0.0,>=1.24.0  # Numerical computing
matplotlib>=3.8.0  # Plotting and visualization for report generation

# Vector search backends
# Primary: OpenSearch with AWS IAM support
opensearch-py>=2.0,<3  # OpenSearch client
requests>=2.31.0  # HTTP library for making requests
requests-aws4auth>=1.2.0,<2  # AWS SigV4 signing for OpenSearch
boto3>=1.20.0,<2  # AWS SDK (for IAM role assumption)

# Legacy: Elasticsearch (deprecated, maintained for backward compatibility)
elasticsearch==8.9.0

# embeddings (prefer fastembed to avoid torch/transformers issues in airflow runtime)
fastembed>=0.3,<1
onnxruntime>=1.16,<2
tokenizers>=0.15,<1

# LLM / embedding providers
openai>=1.0,<2

# Azure Support (Production-grade Azure integration)
# Note: Using standard PyPI packages to avoid Airflow constraints conflicts
# azure-openai not included (use openai directly with Azure endpoint support instead)


# keep compatible with Airflow constraints ecosystem
urllib3<1.27
cffi<2
