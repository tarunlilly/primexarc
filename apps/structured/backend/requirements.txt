# ── Web framework ──────────────────────────────────────────────────────────
fastapi[standard]>=0.115,<0.120
pydantic-settings>=2.4,<3.0
python-multipart>=0.0.9        # multi-file upload support
python-jose[cryptography]>=3.3,<4.0  # JWT validation (RS256 / Azure AD JWKS)

# ── Data profiling ─────────────────────────────────────────────────────────
# Heavy (~250MB installed): brings scipy, scikit-learn, matplotlib, statsmodels
ydata-profiling==4.18.4
pandas>=2.1,<3.0
numpy>=1.26,<2.0

# ── Database connectivity (lightweight stack) ──────────────────────────────
SQLAlchemy>=1.4,<2.0            # ~5MB — pinned <2.0 for sqlalchemy-redshift compat; supports asyncio ext for asyncpg
psycopg2-binary>=2.9,<3.0       # ~4MB — sync Postgres driver (used by source-DB introspection in db_introspector.py)
asyncpg>=0.29,<0.31             # async Postgres driver (used by history_store via SA 1.4 asyncio ext)
alembic>=1.13,<2.0              # schema migration tool — see app/backend/db/migrations/
redshift-connector>=2.1,<3.0    # ~3MB — Amazon's official Redshift driver
sqlalchemy-redshift>=0.8,<1.0   # tiny — dialect adapter for Redshift

# ── Dev / test ─────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.24
pytest-mock>=3.14

pyyaml>=6.0
